# 三轮人工 GT 匿名匹配与可靠性结果（裁决前，2026-09-05）

## 1. 分析口径

本次使用冻结的 Rater 1 第一轮、Rater 1 第二轮和 Rater 2 第一轮原始记录，在管理员后台按同一原图进行匿名技术配对。公开中间结果只保留随机 `adjudication_pair_id`，不输出真实 dataset ID。

叶片对应严格使用测量前锁定的 `phenotype-geometry-v1`：tip/curve/polar 权重为 0.55/0.30/0.15，`tip_norm≤0.12`、总 cost≤0.15，并通过带 dummy 的 Hungarian 算法一对一匹配。可靠性计算使用预锁定公式 `SEM=SDdiff/√2`、`MDC95=1.96×SDdiff`。置信区间固定按植株聚类进行 10,000 次 percentile bootstrap，随机种子为 20260905；该设置在首次查看方法结果前写入实现。

本次只分析人工测量，不载入模型结果，不读取 V4 test。下述结果是裁决前的原始人工重复性证据；裁决只生成独立最终 GT，不覆盖这三轮原始记录。

## 2. 匿名匹配结果

| 比较 | 植株 | 成功匹配 trace | 第一侧未匹配 | 第二侧未匹配 | 说明 |
|---|---:|---:|---:|---:|---|
| Rater 1 第二轮 − 第一轮 | 16 | 35 | 0 | 1 | 第二轮多发现 1 条路径 |
| Rater 2 − Rater 1 第一轮，Core | 12 | 22 | 4 | 0 | Rater 1 有 4 条路径未在 Rater 2 中匹配 |
| Rater 2 − Rater 1 第一轮，Diagnostic | 4 | 8 | 1 | 0 | 仅作逐例和描述性结果 |

## 3. 主要可靠性结果

| 比较与 endpoint | n trace | bias | MDC95 | ICC(A,1) [cluster-bootstrap 95% CI] |
|---|---:|---:|---:|---:|
| Intra，长度 px | 35 | +6.57 px | 21.65 px | 0.9991 [0.9981, 0.9997] |
| Intra，长度 bbox-normalized | 35 | +0.00579 | 0.01920 | 0.9990 [0.9978, 0.9996] |
| Intra，长度 metadata-derived mm | 35 | +0.59 mm | 1.98 mm | 0.9993 [0.9985, 0.9998] |
| Intra，长度对称相对差 | 35 | +0.87% | 3.10% | 不适用 |
| Intra，分化角 | 19 | −1.55° | 15.97° | 0.8955 [0.6429, 0.9661] |
| Inter Core，长度 px | 22 | +17.45 px | 31.87 px | 0.9946 [0.9854, 0.9981] |
| Inter Core，长度 bbox-normalized | 22 | +0.01482 | 0.02772 | 0.9934 [0.9828, 0.9979] |
| Inter Core，长度 metadata-derived mm | 22 | +1.41 mm | 3.06 mm | 0.9972 [0.9931, 0.9992] |
| Inter Core，长度对称相对差 | 22 | +1.92% | 3.72% | 不适用 |
| Inter Core，分化角 | 10 | −1.02° | 17.08° | 0.5090 [−0.1106, 0.8565] |

毫米值均为 `derived from scanner metadata`，实物标尺校准完成前不得解释为经实物验证的绝对长度。主路径分化角按协议为 `not_applicable`，未被错误计为缺失：Intra 有 16 对主路径不适用、Inter Core 有 12 对不适用；真正缺失来自未匹配 trace，分别为 1 和 4。

由预锁定的 `max(MDC95_intra,MDC95_inter)` 得到当前人工测量误差底线：长度对称相对 MDC95 为 **3.72%**，分化角 MDC95 为 **17.08°**。这说明长度描迹非常稳定，而分化角对共同段末端和局部切线细节明显更敏感；后续方法角度差异必须超过约 17.08°，才可解释为超出当前人工测量噪声。

Diagnostic 4 不与 Core 合并。其长度相对 MDC95 为 2.78%，但分化角只有 4 个有效配对，MDC95 为 30.04°、ICC 为负，样本太少且不稳定，仅用于失败机制说明。

## 4. 方法盲裁决门槛

按测量前锁定规则，共有 **13/16 个匿名样本**至少触发一项裁决条件：

- 11 株存在至少一条分化角差异超过 5°；
- 5 株存在可见叶数量或对应身份不一致；
- 1 株存在基部—叶尖结构路径长度对称相对差超过 5%；
- 上述类别存在重叠，不可相加为样本总数。

已生成 Git 忽略的本地裁决入口 `runtime/adjudication/method_blind_20260905/index.html`。每张页面只显示无标注原图和三套按图独立随机为 A/B/C 的人工候选轨迹；不显示测量者、轮次、真实样本编号、模型或数值优劣。裁决者可以接受一套完整候选，或标记“三套均不完整，需要重新描迹”。页面支持滚轮缩放、拖动平移、本地进度、修订和匿名 CSV/JSON 导出。

静态泄漏检查为 0，Edge/Playwright dry run 已完成 13/13 页面提交、CSV/JSON 双导出，浏览器错误为 0。原始三轮人工记录不会被覆盖。

## 5. 当前结论与边界

三轮人工测量已足以量化人工误差底线，但最终人工 GT 尚未冻结。下一步必须先完成 13 张方法盲裁决；如果选择“需要重新描迹”，再单独建立裁决描迹 revision。完成最终 GT 冻结后，才允许执行 Teacher-direct、Student-B 和冻结 Student-D 的表型比较。正式消融、五随机种子与 V4 test 继续后置。
