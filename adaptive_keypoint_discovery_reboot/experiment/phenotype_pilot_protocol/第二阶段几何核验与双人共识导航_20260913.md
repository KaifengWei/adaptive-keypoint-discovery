# 第二阶段几何核验与双人共识导航（2026-09-13）

## 已核验的人工提交

第二位测量者的正式几何导出13/13张均已提交，继承并保持第一阶段30个结构的语义判断。JSON与CSV逐记录一致，图像哈希、匿名包ID及第一阶段导出哈希均匹配；28个结构选了具体候选路径，1个要求重描，1个保留争议。4株的逐叶建议混用了不同候选来源。两份原始文件已原样保存于Git忽略的`runtime/adjudication/semantic_first_20260906/geometry/results/`，并按JSON哈希独立归档于`runtime/admin/semantic_first_20260906/geometry_exports/`。

| 原始导出 | SHA-256 |
|---|---|
| `geometry_decisions.json` | `6e6cccad809bd987230a0f19630411e56dcf45326c2d56e80cdcaaa937c75fcf` |
| `geometry_decisions.csv` | `3c7eadc5d5e2db52363b35ec5e5cfc2e8a8a9bb1947927148839996ff2610d3c` |

这仍是第二位测量者的独立意见，不是最终人工GT。第一位测量者此前选的是**一整组**候选；第二位测量者此轮按**单个结构**选择。以下“来源相同／不同”仅用于找到需要看的图，不能代替逐叶身份匹配、曲线等效性或端点完整性判断。

## 双人并排复核

管理员已生成只读页面：`runtime/adjudication/semantic_first_20260906/consensus_readonly_20260913_v2/index.html`。该文件夹在Git忽略的runtime中，必须连同`images/`一起交给参与共识的两位测量者。页面把未叠线原图、第一位测量者选的整组路径、第二位测量者逐结构建议并排显示；支持1×/2×/4×放大、同株三栏同步滚动。页面不提供“自动接受”或“冻结GT”按钮。

初筛有7张需优先讨论；另外6张候选来源和数量表面一致，也必须确认真实植株、叶尖、基点及路径质量后才可进入共识。

| 原第二阶段页码 | 优先核查原因 |
|---:|---|
| 3 | 第二位测量者逐叶混用两套候选，需核查共同基点及两叶归属。 |
| 5 | 第一位所选整组有4条路径，第二位定位3个结构且混用来源；先裁定是否有多余/漏掉的真实叶片。 |
| 6 | 第二位对一个结构要求重描；此前独立数据质量审计已发现此图的标准化裁剪疑点。先判断端点完整性与可测性，不得把截断端点当真实叶尖。 |
| 7 | 第二位对一个结构保留争议，同时混用候选；先厘清叶片身份，再讨论几何。 |
| 9 | 两位所选候选来源不同，需判断是否实际上几何等效。 |
| 10 | 两位所选候选来源不同，第二位备注认为不止一套可以；需核查等效条件，而非按字母定胜负。 |
| 11 | 第二位逐叶混用候选，需核查共同基点与路径连贯性。 |

两人讨论时按以下顺序记录结论：先确认每个可见结构是否为独立叶片、是否完整可测；再指定每片的路径来源或明确需要重描；最后检查多叶共同基点、共享主路径和端点位置。不能将不同候选曲线直接拼接为GT。若两人无法达成一致，保持待定并请第三位有经验人员裁决。仅有页面上的“来源相同”不能自动判为通过。

## 2026-09-15：正式共同记录入口

在上述只读证据页通过检查后，已生成正式共同记录包：

`runtime/adjudication/semantic_first_20260906/joint_consensus_20260915_v2/index.html`

页面仍显示同一组三栏冻结证据，但现在要求两位测量者共同填写：本图处理结论、共同确认的可见叶片数、逐叶语义状态、唯一完整候选路径或重描/待定、等效候选、信心、理由，以及叶片身份、叶尖、共同基点和共享路径四项检查。两位测量者分别勾选确认后才可提交。不得把不同候选的局部片段拼接成新路径。

三种图级结论分别为：`consensus_ready`（当前共识可作为待冻结候选）、`redraw_required`（至少一叶无完整可用候选）、`third_rater_required`（双方仍无法达成共识）。13张全部完成后导出 `joint_consensus_decisions.json` 和同名CSV并保存到包内 `results/`。管理员须通过构建脚本校验包ID、图像哈希、13张完整性、逐叶约束、双人确认及JSON/CSV一致性。该导出仍不是最终GT；重描或第三方裁决完成并显式冻结前，不进入方法表型比较。

## 冻结边界

本次未读取模型输出作人工GT修订，也未修改Teacher、Student、graph、decoder、阈值、`phenotype-geometry-v1`或MDC95计算。共识记录、必要重描、候选路径与图像质量复核完成前，最终GT仍未冻结；Teacher-direct/B/D表型比较、正式消融、五随机种子和V4 test继续后置。
