# 第一阶段语义核验与第二阶段交接（2026-09-13）

## 当前进度

第二位测量者已完成13张分歧样本的第一阶段独立语义判断。正式导出位于Git忽略的`runtime/adjudication/semantic_first_20260906/semantic/results/`；原始字节另按哈希归档在`runtime/admin/semantic_first_20260906/semantic_exports/`。两处都不是公开分发包，不进入Git。

第一阶段的30个定位结构全部被测量者标为`measurable`：29个高信心、1个中信心；13张均已提交，均声明未见候选曲线，CSV逐行内容与JSON一致。此处“可测”是独立人工意见，不是图像完整性认证。此前原始扫描帧与标准化输入间发现过端点裁剪疑点，仍须在第二阶段逐结构核查；不能据第一阶段的整齐分类自动通过。

| 校验项 | 结果 |
|---|---:|
| 锁定样本与正式提交 | 13/13 |
| 人工定位结构 | 30 |
| `measurable / visible_unmeasurable / uncertain / non_leaf` | 30 / 0 / 0 / 0 |
| 第一阶段候选曲线泄漏 | 0 |
| JSON/CSV记录及摘要不一致 | 0 |
| 原始JSON SHA-256 | `bd4a9fc706ff01a60124ae8cf815fcd8a0197826cdadf05328c4468aa5bf1535` |
| 原始CSV SHA-256 | `2e2c2973501e3019ca9bb7233fd9958a5e81339316050c1ac7d351b5f64ac563` |

## 第二阶段交付

正式页面为`runtime/adjudication/semantic_first_20260906/geometry/index.html`。只向第二位测量者发送整个`geometry/`文件夹，不发送同级`semantic/`、上级`adjudication/`、`runtime/admin/`或第一位测量者的旧意见。第二阶段沿用第一阶段匿名图像及结构定位，语义判断在页面中只读；每张的A/B/C候选重新随机重标，逐结构比较几何。

第二位测量者需依次检查每个已定位结构：候选曲线是否对应同一片真实叶、共同基点与叶尖是否位于可见植株、是否存在截断或错连。候选确实适合时选具体路径；都不适合时选“需要重描”或“保留争议”，并填写理由。若看到第一阶段未发现的端点、身份或可测性问题，在第二阶段备注中明确写出，不回头静默覆盖第一阶段原始导出。多条候选差不多时可在备注说明等效或难分；涉及叶片数量、身份、完整性时不得按A/B/C字母选胜者。

完成全部13张后，从页面分别下载`geometry_decisions.json`和`geometry_decisions.csv`，放在`geometry/results/`，两份一起交回管理员校验。浏览器本地进度不能代替双文件导出。

第二包构建与静态校验：原13张图像资产哈希一致、身份泄漏0、正式`results/`为空、4项Python门禁测试通过。Edge只读打开正式第二包：13张可加载、首页2个结构和3套候选可读、浏览器脚本错误0；未代替人工提交任何判断。第二包绑定第一阶段原始JSON SHA-256，不能用修订或模拟数据静默替换。

## 之后的门槛

第二阶段输出仍只是第二位测量者的独立意见，不是最终GT。管理员再将第一位测量者原意见和第二位测量者两阶段意见按同一原图、同一结构对齐，只将分歧交两人共识；必要时重描或请第三位有经验人员裁决。特别核查裁剪、缺损及共同基点歧义。双人共识和最终GT冻结前，不运行Teacher-direct/B/D表型比较；冻结模型、几何算子、MDC95定义、正式消融、五随机种子及V4 test保持原边界。本次未修改实验模型或运行训练。
