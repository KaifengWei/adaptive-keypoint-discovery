# Rater 1第一轮修订与冻结审计（2026-09-04）

## 1. 审计目的与边界

本次只对Rater 1第一轮人工GT的匿名导出进行结构、字段一致性和修订差异审计，不修改人工描迹，不读取V4 test，不载入Teacher、Student或Decoder输出，也不进行方法比较。

2026-08-31原始导出保持原样；2026-09-04修订导出作为第一轮后续可靠性分析的权威版本。原始CSV/JSON、管理员映射及外置冻结清单均位于Git忽略的`runtime/`目录，不进入GitHub。

## 2. 完整性结果

| 检查项 | 结果 |
|---|---:|
| session | 16，全部`submitted` |
| CSV trace | 35 |
| JSON trace | 35 |
| trace状态 | 35条均为`measurable + complete` |
| 重采样曲线 | 35条均为240点 |
| blind ID | 16个，唯一，且与Rater 1第一轮包manifest完全一致 |
| CSV/JSON trace UUID | 35个，集合及逐记录字段一致 |
| session revision | revision 1为14株；revision 2为2株 |

## 3. 两处字段修订

| 匿名顺序 | blind ID | trace | 修订后遮挡 | 修订后插值 | 结果 |
|---:|---|---|---|---|---|
| 8 | `9YAKV63PTF93NB` | GT02 | `minor_interpolated` | `yes` | 一致；保留测量者说明备注 |
| 14 | `JP6SDK2HYFPJ4M` | GT02 | `none` | `no` | 一致 |

修订后35条记录的组合为：`none + no` 33条，`minor_interpolated + yes` 2条；不一致记录为0。

## 4. 与原始导出的差异边界

- 14张非目标图的session、trace CSV记录和JSON记录完全未改变；
- 只有匿名顺序第8、14张进入revision 2；
- revision按整株重新提交，因此两张图的4条trace均更新revision和记录哈希，但两条GT01没有语义变化；
- 两张图的共同基点、人工控制点、叶尖端点、240点重采样曲线、主路径身份、结构路径长度、bbox归一化长度、角度和曲率指标均未改变；
- 2026-08-31原始三文件的既有SHA-256均复核不变，证明原始包未被覆盖。

## 5. 权威修订包哈希

| 文件 | SHA-256 |
|---|---|
| `VPUFUJSD7GCZ_sessions.csv` | `b4beda533f7ee34968d43359afcf4419b59dafd003249c9c92fb5b595c303934` |
| `VPUFUJSD7GCZ_traces.csv` | `a7cbef0b57972ad46d666543ce3492675316785c467276d58c17483169dc1155` |
| `VPUFUJSD7GCZ_traces.json` | `239269340655781a444fc47723835379253b848ea8a8e94b48b19b557f5cb7a5` |

外置冻结清单为`runtime/measurements/rater1_round1/revision_after_field_resolution/freeze_manifest_20260904.json`，其SHA-256为`77392f47b3f7665ff81a7eb7fab5a4d125a3fb105085df6285b82e710f67d280`。原始JSON内部的`gt_freeze_status=not_frozen`不做追溯改写，冻结状态由该外置清单记录。

## 6. 冻结结论与下一门槛

Rater 1第一轮状态确定为`frozen_rater1_round1`。Rater 1第二轮包含16个全新opaque blind ID，与第一轮重合0；2026-09-04距第一轮4天，符合预锁定的3–7天间隔。

当前只能进入Rater 1第二轮独立描迹。第二轮期间不得查看第一轮记录；在Rater 2、匿名跨session匹配、intra/inter-rater可靠性、SEM、MDC95和方法盲裁决完成前，仍禁止Teacher-direct/Student-B/Student-D表型比较、模型修改、正式消融、五随机种子及V4 test。
