# 两阶段人工裁决实施与交接（2026-09-06）

## 本次结论

用户已提交的13张A/B/C结果完整、CSV/JSON一致，现按原始字节及SHA-256归档为“Rater 1独立裁决意见”，不是最终GT。实际选择来源为Rater 2第一轮8张、Rater 1第一轮3张、第二轮2张；未见“全部偏向自己”的表面现象，但此分布不能证明作者身份盲法有效。

旧页面把“叶片存在/身份”和“中心线几何”放在整套候选选择中，无法表达“一个候选的主路径更好、另一个候选的叶片集合更完整”。本次以新增工具实现两阶段独立复核，用户已经同意继续此方向。旧三轮记录、旧裁决页面和已提交结果均保留。

## 当前交付

- 构建、校验、界面、测试及管理员规则集中在 `adjudication_v2/`，不向experiment根目录增加散落脚本。
- 正式第一阶段：`runtime/adjudication/semantic_first_20260906/semantic/index.html`。
- 交给同事的是整个 `semantic/` 文件夹；结果放在该文件夹的 `results/`。
- 实际第一阶段初始为13张、0个预生成结构、0份人工结果；使用重新随机的16位匿名ID和图像顺序。底层13张仍为原裁决集合，图片逐字节不变，不重新选样。
- 包中只有标准化输入图，没有旧路径坐标、旧叶数、分歧类别、身份映射、模型输出或裁决意见。管理员映射位于分发包之外。
- 第二阶段构建器已实现并在临时测试环境验证。正式第二包尚未生成；必须先收到真实第一阶段JSON与同名CSV，通过核验并归档后才发放。

第一阶段动态添加结构定位圈，并记录完整可测、存在不可测、身份不确定或确定非叶片。定位圈只用于指明被讨论的组织，不是新路径测量，不进入模型训练。共享主路径和中转控制点不等于遮挡插值；缺少真实叶尖或共同基点的路径不得记为完整可测。

第二阶段按结构选择A/B/C中的具体路径，或提出重描、保留争议。原语义判断只读；新发现写入第二阶段备注。两套结果作为独立意见交由两人共识，必要时请第三人裁决。混合候选的共同基点和路径一致性仍须检查；程序不会自动拼成GT。原测量者两轮不能当作两名独立专家的多数票。

## 图像完整性问题的处理

旧匿名例 `MTU2U5NHQQZ3KK` 的原始大幅扫描图显示有第二片叶，标准化图只保留了它的截断片段。因此旧A的一路径选择不能独自解决叶片集合；旧B/C的第二路径也不能把截断端当作完整叶尖。

此前聊天中将该株长主路径直接判作完整可测的表述不够严谨：标准化图左端同样存在平截外观。现在不自动接受任何可疑端点，也不改写原始人工结果。所有叶片按同一可测性规则重新确认；完整原始扫描帧仅作管理员数据质量证据，不把模型输入之外的端点补进评价GT，也不据此替换冻结输入图像。

原三轮可靠性统计继续保留其“裁决前”身份。高重复性不能证明裁剪完整，也不能证明植物学身份正确。若最终裁决确认了截断，后续需显式记录端点可测性与有效样本数，必要的敏感性分析单独报告；不静默覆盖MDC95原始计算、样本分组或冻结方法。

## 验收证据

| 检查 | 实际结果 |
|---|---|
| 原13张裁决归档 | CSV/JSON 13条一致；归档与原文件SHA-256一致 |
| 第一阶段分发清单 | 13张原图哈希保持一致；身份泄漏0；候选数据不存在 |
| Python门禁测试 | 4项通过，覆盖裁剪不可作为完整路径、坐标边界、伪造提交条件和第二阶段语义不可改写 |
| Edge离线dry run | 临时目录完整执行13张第一步＋13张第二步；4个下载文件；浏览器错误0 |
| 修订与恢复 | 修订保留历史；刷新恢复、JSON备份恢复通过 |
| 导出核验 | 两阶段模拟JSON/CSV逐条一致；第二步保持第一步语义内容 |
| 正式人工数据 | 第一阶段results为空；正式第二阶段未生成；模拟结果只在系统临时目录 |

本地验收报告与预览另存 `runtime/adjudication/semantic_first_20260906/qa/`，不放入同事分发包。模板、协议和验证代码进Git；真实人工原始导出、匿名映射和分发图像继续由runtime忽略规则隔离。

## 下一步

由原第二位测量者独立完成第一步定位与分类，不需重新画16张或重做三轮。收到两份文件后，运行构建器发放第二阶段逐叶几何包；两位测量者仍有分歧的项目再进入共识记录及必要重描。最终人工GT冻结后才能进行Teacher-direct/B/D表型比较。

本次未修改Teacher、Student、graph、decoder、冻结几何函数或阈值，没有训练、模型推断或V4 test读取。
