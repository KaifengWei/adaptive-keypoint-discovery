# 迁移到新 RTX 3090 服务器执行记录

日期：2026-08-01

## 1. 迁移边界与连接方式

- 源服务器：SSH 别名 `cv`，`neaucs2@192.168.2.169`；
- 新服务器：`neaucs2-C246-WU4`，NVIDIA GeForce RTX 3090 24 GB；
- 新服务器工作根目录：`/home/neaucs2/Desktop/kf`；
- 办公室内使用 SSH 别名 `kf3090`，连接局域网地址 `192.168.2.141`；
- 异地使用别名 `kf3090-tail`，连接同事 Tailnet 分享的 Tailscale 地址 `100.112.27.64`；
- Tailscale 设备归属和分享关系未改变。目标机没有切换到用户自己的 Tailnet；
- 不删除 `cv` 文件，不撤销或迁移 GitHub deploy key，不读取 V4 test。

本机经 Tailscale 连接目标机时实测约 551 ms，而同一局域网地址低于 1 ms。VS Code 终端输入卡顿主要来自前者。办公室内应关闭原远程窗口，再通过 `Remote-SSH: Connect to Host...` 选择 `kf3090`；异地改选 `kf3090-tail`。

## 2. 已迁移资产与完整性

### 完整工程

- 源路径：`/home/neaucs2/kp/adaptive-keypoint-discovery`；
- 目标路径：`/home/neaucs2/Desktop/kf/adaptive-keypoint-discovery`；
- 首轮服务器间直传包含 `.git`，共 5,896 个普通文件、911,388,584 字节；
- 排除 `.git` 后，源端与目标端同刻核验均为 5,408 个文件、768,745,928 字节；
- 两端按路径排序、逐文件 SHA-256 后的聚合摘要均为 `8eac63fef7d0b37910888f785291c4afb70bdad1c80ceb468616054f69a5d501`；
- `git fsck --full` 通过；
- `cv` 的 `c34210d` 已先完整迁移，再由本机离线 Git bundle 快进到 `1b66448`；本迁移记录及兼容修复随之后续仓库提交同步；
- 本机尚未提交的 `manual_phenotype_reference_pending.csv` 作为工作树修改单独保留，迁移时两端 SHA-256 均为 `9eb541df37cb7751228341b8cecea2a35d9bfa9a3feab9743a342f8a76f54b1f`。

6 个关键权重均已在目标机核验存在：

- `training_outputs/core_dinov2/best.pt`；
- `training_outputs/core_dinov2_v4_fullplant/best.pt`；
- `training_outputs/core_dinov2_v4_phenotype_roi/best.pt`；
- `training_outputs/core_dinov2_v4_structure_coverage/best.pt`；
- `training_outputs/preflight_step1/best.pt`；
- `third_party/checkpoints/dinov2_vits14_reg4_pretrain.pth`。

V4 图像目录在目标机重新计数为 train 220、val 40、test 40。test 只做文件数量核验，没有传入模型命令。

### CUDA 离线依赖

- 源路径：`/media/neaucs2/evs/wheelhouse/adaptive_kp_cu128`；
- 目标路径：`/home/neaucs2/Desktop/kf/wheelhouse/adaptive_kp_cu128`；
- 18 个文件、4,066,944,384 字节；
- 源端与目标端聚合 SHA-256 均为 `c3a6244a96865c9142276cab494cf14ff4d1df49c80fb658ed4c409bc3d33aef`。

## 3. 个人 Conda 环境

服务器原先没有 Conda。本次只在用户目录安装，不改系统 Python：

- Miniconda：`/home/neaucs2/Desktop/kf/miniconda3`；
- 环境名：`kf`；
- 环境路径：`/home/neaucs2/Desktop/kf/miniconda3/envs/kf`；
- 包源：`conda-forge`，严格优先；没有代替用户接受 Anaconda defaults 服务条款；
- Python 3.12.13；
- PyTorch `2.9.1+cu128`；
- torchvision `0.24.1+cu128`；
- CUDA 12.8；cuDNN 9.10.2；
- GPU：RTX 3090 24,576 MiB，驱动 595.84，计算能力 8.6。

进入环境：

```bash
source ~/Desktop/kf/miniconda3/etc/profile.d/conda.sh
conda activate kf
unset LD_LIBRARY_PATH
cd ~/Desktop/kf/adaptive-keypoint-discovery/adaptive_keypoint_discovery_reboot/experiment
```

`setup_remote_env.sh` 已改为默认使用 `conda-forge`，并允许通过 `CONDA_CHANNEL` 覆盖。三个 V4 train/val 启动脚本已改为默认使用当前环境的 `python`，不再写死旧服务器路径。

## 4. GPU、DINOv2 与训练验收

1. `remote_gpu_check.py` 通过：CUDA 可见，cuDNN 初始化成功，最小卷积成功；
2. DINOv2 ViT-S/14-Reg 本地源码和预训练权重成功加载；
3. V4 结构覆盖配置 dry-run 成功：182 张训练、34 张内部验证，输入 `4×3×518×518`，输出热图 `4×1×129×129`，可训练参数 830,401，人工关键点标签使用状态为 false；
4. 完成 1 个真实优化步骤及 34 张内部验证：train loss 11.0956，validation loss 11.4616，临时 `best.pt` 为 91,613,451 字节；该结果只证明训练链路完整，不作为论文性能结果；
5. 所有冒烟产物写入 `/tmp/kf_migration_*`，未覆盖历史 checkpoint 或评估输出，未读取 test。

首次用 `num_workers=4` 时，Linux 默认 `fork` 在 DINOv2 已初始化 CUDA 后导致 DataLoader 等待。训练入口已改为：当 worker 数大于 0 时使用 CUDA 安全的 `spawn` 上下文。原 4-worker 配置随后完成 dry-run 和真实单步反向传播；该修复只改变数据加载进程的启动方式，不改变模型、数据、损失或指标。DINOv2 提示 xFormers 未安装时会使用官方回退实现，不影响本次前向与反向验收。

## 5. 当前结论

- 新服务器已经具备继续 train/val 方法开发实验的完整条件；
- `cv` 仍保留完整源副本和原 GitHub 连接，当前不做下线；
- 新服务器尚未连接或替换 GitHub deploy key，等待用户另行确认；
- 下一步仍按项目状态做平台四二维诊断和一般规则修订，不因换机改变研究路线；
- 人工表型参考、正式消融、五随机种子和 V4 test 继续后置。
