# 方案C独立人工路径复核结果

日期：2026-07-25

数据范围：V4 val 40张

复核来源：`experiment/manual_path_review_completed.csv`

V4 test读取数：0

## 1. 复核完整性

- 复核行数：40/40；
- 唯一图像编号：40；
- 相对平台三待复核清单：无缺失、无额外编号；
- 四个分类字段均已填写；
- 备注为空26张，其余14张提供了位置或错误说明。

该CSV按用户原始判断保留，不根据自动指标回改人工字段。

## 2. 方案C独立复核结果

| 指标 | 结果 |
|---|---:|
| 路径语义 pass / fail | 36 / 4 |
| 漏叶 no / yes / uncertain | 32 / 7 / 1 |
| 错连 no / yes | 33 / 7 |
| 基部 correct / wrong | 34 / 6 |
| 严格联合通过 | 26/40（65.0%） |

严格联合通过定义为同时满足：

1. `manual_path_semantics=pass`；
2. `manual_missing_organ=no`；
3. `manual_wrong_connection=no`；
4. `manual_base_selection=correct`。

预先锁定的工程门槛为严格联合通过至少32/40、漏叶不超过4张、错连不超过3张、基部wrong不超过2张。方案C四项均未达到门槛，因此当前不能进入人工长度、角度和曲率参考，也不能启动正式消融、五随机种子或V4 test。

## 3. 与路线B的同口径比较

| 指标 | 路线B | 方案C | 变化 |
|---|---:|---:|---:|
| 路径语义 pass | 30 | 36 | +6 |
| 漏叶 yes | 8 | 7 | -1 |
| 错连 yes | 5 | 7 | +2 |
| 基部 wrong | 4 | 6 | +2 |
| 严格联合通过 | 22 | 26 | +4 |

配对到同一批40张val图后：

- 7张由非严格通过变为严格通过：`v4_val_0002`、`0013`、`0020`、`0025`、`0028`、`0036`、`0039`；
- 3张由严格通过变为非严格通过：`v4_val_0019`、`0033`、`0038`；
- 其余30张严格状态不变。

这说明“结构覆盖增强教师 + 局部尺度解码”带来了净4张严格通过增益，但新增路径并不全是真实器官路径：漏叶仅小幅减少，同时错连和基部偏差增加。不能只依据路径语义pass或路径总数宣布方案C通过。

## 4. 主要失败模式

1. **零路径或关键结构整体缺失**：`v4_val_0012`、`0023`被判为路径fail、漏叶、错连和基部wrong。
2. **叶片只恢复局部或漏掉小叶**：`v4_val_0040`、`0019`、`0030`、`0014`、`0005`。
3. **路径经过分叉但没有把侧叶解码成独立器官**：`v4_val_0015`。
4. **重叠叶片的器官身份不确定并发生错连**：`v4_val_0038`。
5. **shoot侧基部仍有偏移或落入根须方向**：`v4_val_0031`、`0035`、`0034`、`0003`，以及两个零路径样本。

`v4_val_0031`的自动结果为`decoded_path_count=0`、`base_node_id=-1`，但人工字段记录为路径pass、无漏叶、无错连、基部wrong。该行可能是复核者将左侧“学习点结构图”的覆盖视为路径可用，而右侧最终器官路径实际为空。原始人工判断不改写，但在最终裁决或第二复核者一致性检查时应列为重点样本。即使将其路径语义改判为fail，严格联合通过数仍为26/40，不改变当前门槛结论。

## 5. 当前决策与下一步

1. 方案C保留为有增益但未通过工程门槛的开发版本，不冻结为论文最终方法；
2. V4 test继续锁定，表型准确率保持`pending`；
3. 独立复核已经完成，现在可以打开平台四做教师×解码器二维诊断；
4. 平台四只用于判断错误主要来自结构覆盖教师还是局部尺度解码器，不把人工判断变成关键点坐标标签；
5. 后续改动必须形成跨样本的一般规则，不为单张val图添加例外；
6. 完成二维诊断后再决定最小修订方向，并重新在val上验证是否达到预设门槛。
