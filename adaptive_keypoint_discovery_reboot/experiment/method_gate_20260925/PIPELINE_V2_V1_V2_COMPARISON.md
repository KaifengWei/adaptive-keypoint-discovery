# Pipeline V1 / V2 一次性比较（V4 val，2026-09-25）

本比较严格采用 `PIPELINE_V2_PREREGISTRATION.md` 第5–6节；先封存40株×2方法的V2完整输出，再读取冻结16株GT。比较属于受先前val失败启发的**开发裁决**，不是独立test验证。Teacher-direct与第53轮Student-B各自和自己的V1配对，绝不跨方法偷换点源或下游。权威逐点、逐图、逐路径、逐GT配对记录位于Git忽略的 `experiment/phenotype_pilot_protocol/runtime/pipeline_v2_20260925/`；完整GT裁决JSON SHA-256=`4e61c27daca1e7b6315208d43a06511af9a6e16d389944b3fcf5e2cf5fba7a59`。

## 1. 40株接口与拓扑

| 冻结点源 | V1→V2接受节点 | 原超距点被V2真正入图／进入最终路径 | V1已接受节点被V2拒绝 | V1→V2路径总数 | 零路径图 V1→V2 |
|---|---:|---:|---:|---:|---:|
| Teacher-direct | 254→206 | 5 / 5 | 53 | 96→54 | 1→8 |
| Student-B | 143→112 | 3 / 3 | 34 | 76→43 | 2→11 |

逐点拒绝口径：Teacher V1超距10，V2加权拒绝5、冻结ROI拒绝53、地下部独占拒绝0；Student V1超距17、重复合并1，V2加权拒绝15、ROI拒绝33、地下部独占拒绝1、重复合并0。V2使8个旧超距点入图且这些点位于最终路径上，但**没有任何同株同点源的原零路径或基部失败因此解除**。相反，Teacher新增7张、Student新增9张零路径图。没有新 `U∧¬H` 接受节点或路径像素污染。完整变化点ID、`d/D`、`δ`、`r`、`A_i`、旧/新原因及路径参与保存在 `point_associations.jsonl`、`graphs.jsonl` 与 `GT_COMPARISON_AND_GATE.json`，不能把“入图”本身当作真实叶片恢复。

两个先前已知的Student基部困难例没有被V2解决：`v4_val_0002/p02` 新分数约0.02，低于固定 `exp(-2)≈0.1353`；`v4_val_0031/p01` 约0.13，同样低于该界限。后者接近界限**不构成**改公式或补丁理由。相反，冻结ROI硬条件排除了大量V1已有的学习节点，这是本次净退化的主要接口现象，不得回头扫描ROI、宽度或分数阈值。

## 2. 最后读取的冻结16株GT：Core和Diagnostic分列

表中“匹配/几何GT”是冻结 `match_cross_session` 的可测路径匹配；“预测路径”是全部输出路径。路径精度下界=`匹配/预测`，不把无曲线的独立结构强判为假阳性。

| 点源 / 组别 | 可测GT | V1匹配 / 预测 | V2匹配 / 预测 | V1→V2精度下界 | V1→V2漏配 |
|---|---:|---:|---:|---:|---:|
| Teacher / Core 12 | 24 | 20 / 24 | 12 / 13 | 0.833→0.923 | 4→12 |
| Teacher / Diagnostic 4 | 9 | 9 / 12 | 6 / 7 | 0.750→0.857 | 0→3 |
| Student / Core 12 | 24 | 20 / 22 | 15 / 16 | 0.909→0.938 | 4→9 |
| Student / Diagnostic 4 | 9 | 7 / 9 | 5 / 6 | 0.778→0.833 | 2→4 |

V2的“精度下界升高”只发生在**预测数大幅减少**的条件下；四个方法×组的GT匹配数全部下降，不能称为表型收益。共同匹配GT的配对长度误差中位变化和角度误差中位变化均未触及原MDC95警戒线，但它们只在保留下来的匹配路径上计算，不能抵消消失的真路径。V2新产生了3个按预定义 `>0.025D` 的GT共同基点偏离事件：Teacher Core `v4_val_0009`、`0016`，Student Core `v4_val_0035`。未观察到新增已确认假侧枝或新地下部污染，不影响先行安全门槛的裁决。

逐株配对、匹配GT身份、长度px与/D及metadata-derived mm、分化角、未匹配预测区间、基部距离px与/D、成对误差及全部安全标志保存在上述私有JSON。毫米数仍仅为 *derived from scanner metadata*，尚未经已知尺寸标尺校验；曲率仍为探索性，不参与淘汰。此处“路径长度”始终指**基部—叶尖结构路径长度**，不是植物学叶长。

## 3. 对预注册门槛的逐项判定

- 安全门槛已触发：新增零路径、Core/Diagnostic四组均丢失可测GT匹配，且出现新基部偏离事件。因此先判 `RESULT 3`，无需把较高的条件性精度或长度中位数作为“救回”证据。
- 两株不同原始扫描帧的“超距点入图并解除同株基部/零路径失败”条件为**否**；16株至少新增一条正确GT匹配也为**否**。
- 任一组GT匹配数和精度下界同时不下降条件为**否**。未匹配预测上界未升高，且共同匹配路径的MDC95警戒未触发；这两项不改变先行安全裁决。
- V4 test读取=0、预注册=0；没有二次V2调参/运行、V3、Student重训或冻结GT修改。最终回退V1，详见 `FINAL_PIPELINE_DECISION.md`。
