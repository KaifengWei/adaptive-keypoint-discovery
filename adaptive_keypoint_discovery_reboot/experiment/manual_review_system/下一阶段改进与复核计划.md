# 下一阶段冻结与复核计划

更新时间：2026-08-02

## 当前冻结状态

A/B/C/D四个现有组合立即冻结：不修改Teacher、Decoder或阈值，不增加方案E/F，不重新训练，不读取V4 test。方案D不再带“推荐候选”身份。

| 方案 | 自动教师 | 路径解码器 |
|---|---|---|
| A | 路线B教师 | 全局尺度 |
| B | 路线B教师 | 局部尺度 |
| C | 结构覆盖增强教师 | 全局尺度 |
| D | 结构覆盖增强教师 | 局部尺度 |

自动结果显示A/B/C/D总路径数为64/76/63/74，说明局部尺度Decoder是路径增加的主要来源；新Teacher没有表现出自动路径数净增益。但路径数量不是语义正确性的证据。

## 最后一轮路径语义复核

平台四逐图完成：

- A、B、C、D各自的真实器官路径数、漏叶数、假枝数、错连和基部；
- A vs B：旧Teacher下Decoder贡献；
- A vs C：全局Decoder下Teacher贡献；
- C vs D：新Teacher下Decoder贡献；
- B vs D：局部Decoder下Teacher贡献；
- 每图最佳方案、并列、均不可用和D相对A的变化。

导出`factorized_method_review_completed.csv`后，运行：

```powershell
python .\analyze_factorized_review.py D:\path\factorized_method_review_completed.csv
```

人工结果只作路径语义评价，不作为关键点训练标签，不据此为单张图增加规则。

## 表型优先阶段

旧的“严格联合通过至少32/40后才建立人工表型参考”门槛取消。A/B/C/D配对复核完成后，无论路径通过率是否达到32/40，都先提出一个模型盲的小型V4 val表型参考方案，等待用户确认后实施。

下一阶段至少比较：

1. 传统几何基线；
2. G1′ Teacher-direct；
3. Student配候选Decoder。

模型选择以叶长、倾角和必要时曲率的人工参考误差为主；路径语义只解释误差来源。Teacher-direct与Student对照完成前，不默认Student具有额外价值。

## 当前禁止事项

- 新Teacher规则、新Decoder阈值或单图例外；
- 增加epoch、五随机种子或大规模消融；
- V4 test；
- 宣称C/D优于B、Student优于Teacher或表型已经验证；
- 直接实现Slot Attention、关系网络或其他大型新架构。

完整证据见`../当前研究路线证据审计与下一阶段冻结计划_20260802.md`。
