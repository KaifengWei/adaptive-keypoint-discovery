# 下一阶段改进与复核计划

## 当前证据

路线B将旧路线基部错误从28张降到4张、错连从11张降到5张，说明地上部有效域和shoot侧基部约束有效；但漏叶仍为8张，严格联合通过为22/40。

val-only阈值扫描表明，降低检测阈值主要增加冗余节点，不能系统补回小叶路径。因此下一轮不以“多取点”为主。

## 已确认的技术方向

下一轮采用“结构覆盖增强自动教师 + 局部尺度短枝解码”：

1. 自动识别通过持久性筛选的地上部终端分支；
2. 为真实小叶终端保留跨增强稳定候选；
3. 为shoot侧基部过渡区保留自动结构候选；
4. 短枝仅在已有学习点支持时，结合点置信度、局部叶宽和局部分支长度保留；
5. 禁止骨架自动补造没有学习点支持的叶尖；
6. 不规定固定点数，不使用人工关键点坐标。

## 首轮方案C自动结果

- 216张train教师目标点数中位数由8增至9，每株最多一个identity视图基部覆盖角色；
- 80轮同种子训练最佳内部验证损失0.3312；
- 40张val点数中位数4，重复性F1中位数0.933；
- 组合方案输出74条候选路径，旧路线为64条；
- 已知漏叶中`v4_val_0002`、`0015`、`0039`出现路径恢复，但`0012`、`0023`、`0031`为零路径失败；
- 自动结果只能说明“值得复核”，不能说明新增路径都是真叶。

## 方案C独立复核执行结果（2026-07-25）

- 平台三40张均已完成，编号完整且四个分类字段无缺失；
- 路径pass/fail=`36/4`，漏叶yes=`7`、错连yes=`7`、基部wrong=`6`；
- 严格联合通过26/40，相对路线B的22/40净增4张；
- 7张严格状态改善、3张退化，说明新增路径同时包含真叶恢复和新的错连/基部偏差；
- 未达到严格通过32/40、漏叶不超过4张、错连不超过3张、基部wrong不超过2张的预设门槛；
- `v4_val_0031`自动结果为零路径和基部失败，但人工字段为路径pass、无漏叶、无错连、基部wrong，需在第二复核或最终裁决中重点确认；不改写原始CSV。

完整统计见`../方案C独立人工路径复核结果_20260725.md`。

## 下一轮复核安排

1. 现在打开平台四，定位教师与解码器各自对真叶恢复、错连和零路径失败的贡献；
2. 如有第二名复核者，先让其独立完成平台三，再计算一致性并共同裁决分歧；
3. 根据二维诊断只提出跨样本的一般规则，不为单张val图设置例外；
4. 修改后仍只使用train训练、val选择，继续执行同一工程门槛；
5. 通过后才建立人工长度、角度和曲率参考；
6. 消融、五随机种子和V4 test继续后置。

该门槛用于决定是否进入表型测量，不是论文最终测试结论。
